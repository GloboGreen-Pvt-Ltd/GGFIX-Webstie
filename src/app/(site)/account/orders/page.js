'use client';

import OrdersExperience from '@/components/site/account/OrdersExperience';

/** Customer app My Orders flows, adapted to the public website account area. */
export default function MyOrdersPage() {
  return <OrdersExperience />;
}
